[NEAT]
fitness_criterion      = max
fitness_threshold      = 20000.0
pop_size               = 200
reset_on_extinction    = False
no_fitness_termination = False

[DefaultGenome]
# --- Input / Output ---
# Input: 26 (vettore RAM+ velocità+ wall position) + 100 (griglia visiva 10*10) = 126
num_inputs              = 126
# Output: 5 azioni (NOOP, UP, RIGHT, LEFT, DOWN) per Pacman
num_outputs             = 5

# --- Struttura Rete ---
num_hidden              = 20
feed_forward            = True
initial_connection      = full_direct

# --- Compatibilità (I parametri mancanti che causavano l'errore) ---
# Questi coefficienti dicono a NEAT quanto due genomi devono essere diversi
# per essere considerati di specie diverse.
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient   = 0.5

# --- Attivazione ---
activation_default      = tanh
activation_mutate_rate  = 0.1
activation_options      = tanh

# --- Aggregazione ---
aggregation_default     = sum
aggregation_mutate_rate = 0.0
aggregation_options     = sum

# --- Bias (Nodi) ---
bias_init_mean          = 0.0
bias_init_stdev         = 1.0
bias_init_type          = gaussian
bias_replace_rate       = 0.1
bias_mutate_rate        = 0.7
bias_mutate_power       = 0.5
bias_max_value          = 30.0
bias_min_value          = -30.0

# --- Risposta ---
response_init_mean      = 1.0
response_init_stdev     = 0.0
response_init_type      = gaussian
response_replace_rate   = 0.0
response_mutate_rate    = 0.0
response_mutate_power   = 0.0
response_max_value      = 30.0
response_min_value      = -30.0

# --- Pesi (Connessioni) ---
weight_init_mean        = 0.0
weight_init_stdev       = 1.0
weight_init_type        = gaussian
weight_replace_rate     = 0.1
weight_mutate_rate      = 0.8
weight_mutate_power     = 0.5
weight_max_value        = 30.0
weight_min_value        = -30.0

# --- Abilitazione Connessioni ---
enabled_default         = True
enabled_mutate_rate     = 0.01
# Parametri per la probabilità di aggiungere/rimuovere nodi e connessioni
# Fondamentali per l'evoluzione della topologia
node_add_prob           = 0.2
node_delete_prob        = 0.2
conn_add_prob           = 0.5
conn_delete_prob        = 0.5

[DefaultSpeciesSet]
compatibility_threshold = 4

[DefaultStagnation]
species_fitness_func = max
max_stagnation       = 12
species_elitism      = 2

[DefaultReproduction]
elitism            = 2
survival_threshold = 0.2
min_species_size   = 2